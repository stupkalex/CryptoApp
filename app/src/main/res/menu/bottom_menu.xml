<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/list"
        android:icon="@drawable/list_logo"
        android:title="@string/coin_list_text"
        android:checked="true"
        android:color="@color/home"/>

    <item
        android:id="@+id/profile"
        android:icon="@drawable/profile_logo"
        android:title="@string/profile_text"
        android:color="@color/home"/>

    <item
        android:id="@+id/trade"
        android:icon="@drawable/deal_logo"
        android:title="@string/trade_text"
        android:color="@color/home"/>
</menu>